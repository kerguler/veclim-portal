export const timeseries = {
  // vec param to send to backend
  vecParam: 'albopictus',
  // dynamic = use getDateRange()
  dateMode: 'dynamic',
  // optional: opr override if ever needed
  opr: 'ts',
};
